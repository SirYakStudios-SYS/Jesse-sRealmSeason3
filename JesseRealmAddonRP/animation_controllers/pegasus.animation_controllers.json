{
	"format_version" : "1.17.30",
	"animation_controllers" : {
		"controller.animation.flying_animation" : {
			"initial_state" : "flying",
			"states" : {
				"flying" : {
					"animations" : [ 
						{ "flying": "!query.is_on_ground"} 
					],
					         "transitions": [
							{"idle" : "query.is_on_ground"}
					]
				},
				"idle" : {
					"animations" : [ "idle" ],
					"transitions" : [
						{
							"flying" : "!query.is_on_ground"
						}
					]
				}
			}
		}
	}
}